

Math.random() // returns a number between 0 and 1

Math.floor() // returns the largest integer less than or equal to a given number

Math.floor(Math.random()) // always 0

Math.floor(Math.random()*10) // before 10

var list = []  //empty array
list.length
var list = ["hello"]
list.length
list[0]
list.push["there"]  // To add an item to an array
list.length
list[0]
list[1]

var rando = new function(){
    function getRandomInt(x){
        return Math.floor(Math.random()*x)
    }
}

Date.now() // current date and time represented in milliseconds
